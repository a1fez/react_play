## Just React playground to learn React

# Notes 

- created react test project, learned how to use useState, what means JSX and figured out with "setCount" function

-created some own UI. deleted original React UI, added own div and button without functions